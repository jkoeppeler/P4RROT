[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"


[tool.pytest.ini_options]
log_cli = true
log_cli_level = "info"
#addopts = "--cov=p4rrot"
testpaths = [
    "tests",
]
